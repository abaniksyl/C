#include <stdio.h>
int main () {
    int num,fibo,count=0,first=0,second=1;
    printf("Enter a range : ");
    scanf("%d",&num); 
    for (i=0;i<num;i++) {
        if (count<=1){
            fibo=count;
        }
        else {
            
        }

    }

    return 0;
}